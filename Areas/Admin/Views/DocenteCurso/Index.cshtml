@model IEnumerable<LICEORURALJASMINEZB.Models.DocenteCurso>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NivelDetalleCurso)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Docente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Estado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NivelDetalleCurso.IdNivelDetalleCurso)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Docente.Apellidos)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Estado)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdDocenteCurso">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdDocenteCurso">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdDocenteCurso">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
